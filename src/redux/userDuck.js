// Constants
let initialData = {
  loggedIn: false
}
// let LOGIN = "LOGIN"

// Reducer
export default function reducer(state = initialData, action) {
  switch(action.type) {
    default:
      return state
  }
}

// Action (action creator)